language: node_js
node_js:
  - "12.16.1"

cache: yarn

matrix:
  include:
    - name: "Server Pipeline"
      env: TARGET=packages/simple-script-server

script:
  - cd $TARGET
  - yarn lint
  - yarn test
  - yarn build
